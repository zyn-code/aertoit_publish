# Image assets

Missing on purpose — these belong to Aertoit and live on `framerusercontent.com`
on the current site. Export the originals and drop them here.

| File | Used by | Notes |
|------|---------|-------|
| `hero-toiture.jpg` | homepage hero | Full-bleed roof photo. Export ~2048px wide WebP + JPEG fallback. Sits under a navy scrim, so mid-tone contrast is fine. |
| `logo-aertoit.svg` | header | White wordmark with the roof-peak accent over the "o". |
| `certifications/*.svg` | "Les plus d'AERTOIT" | RGE QUALIBAT, VELUX Installateur Conseil Expert, RGE Éco Artisan, plus the two custom blue icons. |

Until `hero-toiture.jpg` exists the hero renders on its solid navy fallback
(`--c-hero-navy`), which is legible but flat.
